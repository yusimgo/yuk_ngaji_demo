import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(theme => ({
  daftarBox: {
    flexDirection: "row"
  },
  logoBox: {
    width: 380,
    height: 111
  },
  menuBarisSatu: {
    flexDirection: "column",
    padding: "10px",
    alignItems: "center"
  }
}));
